import React,{useContext} from "react";



const Products = ({image, price,category,title,description}) => {

    const [disptch,state]=useContext(Con)
    return (
        <div>

        </div>
    )
}

export default Product;